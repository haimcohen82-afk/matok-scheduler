# Claude onboarding — MATOK AI HUB

Before performing MATOK work:

1. Connect the same Google Drive account used by MATOK.
2. In Drive, open and read `MATOK AI HUB - MASTER CONTEXT` in full.
3. Treat that document as the shared business context. A newer explicit instruction from Haim overrides it.
4. Read `MATOK AI HUB - ACTION QUEUE` before executing operational tasks.
5. For Meta tasks, execute only queue items intended for Claude and update their status after completion.
6. Never invent prices, campaign conditions, logos, products or publication times.
7. MATOK creative work must use the approved MATOK design envelope; do not replace it with generic Canva styling.
8. Do not publish unless the final asset and copy are approved and the MCP write tool requests the expected explicit confirmation.
9. After the remote MCP is connected, use `connection_status` first. Only then test a controlled post.
10. On mobile, use the remote `MATOK Meta Connector` as the execution arm. ChatGPT remains the primary planning/creation layer unless Haim instructs otherwise.
